# tushare Pro token（2000 积分），必填；部署时走服务器环境变量，勿提交进版本库
TUSHARE_TOKEN=

# 数据目录（行情 duckdb、业务 sqlite、回测结果），默认仓库根目录下 data/
# DATA_DIR=../data

# tushare 调用节流：每分钟最大调用次数，保守默认 90
# TUSHARE_MAX_CALLS_PER_MINUTE=90
